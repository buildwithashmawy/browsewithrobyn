# deps
node_modules/
.pnpm-store/

# builds
.next/
out/
dist/
.turbo/

# env
.env
.env.local
.env*.local

# convex local state
.convex/

# convex generated code IS committed so production builds (Railway) don't run
# codegen; regenerate locally with `npx convex dev` / `pnpm codegen`.

# playwright
apps/agent/.pw-user-data/
playwright-report/
test-results/

# misc
*.log
.DS_Store
.gstack/
